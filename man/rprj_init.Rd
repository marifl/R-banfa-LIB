% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prj_init.R
\name{rprj_init}
\alias{rprj_init}
\title{Initialize a new R project}
\usage{
rprj_init(path = NULL, name = NULL, template = "base", open = FALSE)
}
\arguments{
\item{path}{Character string. Path where the project should be created.
If NULL (default), uses the current directory.}

\item{name}{Character string. Name of the project. If NULL (default),
uses the directory name.}

\item{template}{Character string. Name of the template to use.
Default is "default". Use \code{rprj_list_templates()} to see available templates.}

\item{open}{Logical. Should the project be opened in RStudio after creation?
Default is FALSE.}
}
\value{
Invisibly returns the path to the created .Rproj file.
}
\description{
Creates a new R project with a .Rproj file based on a template.
}
\examples{
\dontrun{
# Create a project in the current directory
rprj_init()

# Create a project with a specific name
rprj_init(name = "my_analysis")

# Create a project in a specific directory
rprj_init(path = "~/projects/new_project", name = "awesome_project")

# Use a custom template
rprj_init(template = "shiny_app")
}
}
